AUX_SOURCE_DIRECTORY(${CMAKE_CURRENT_SOURCE_DIR} SRC)

ADD_LIBRARY(common_static STATIC ${SRC})

TARGET_INCLUDE_DIRECTORIES(common_static
   PRIVATE
      ${CMAKE_CURRENT_SOURCE_DIR}
)

TARGET_LINK_LIBRARIES(common_static

)
